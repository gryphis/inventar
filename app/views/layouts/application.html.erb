<!DOCTYPE html>
<html title="huhu">
<head>
  <title><%= @title || "Inventar" %></title>
  <!-- %= stylesheet_link_tag    "application", :media => "all" % -->
  <%= javascript_include_tag "application" %>
  <%= javascript_include_tag "form_helper" %>
  <%= csrf_meta_tags %>
  <style type="text/css">
*		{ padding: 0; margin: 0; }
/* html		{ background: #f8ffff; } */
.flash		{ padding: 10px;
		  font-weight: bold;
		  background: #fee; 
		}
.errorExplanation { background: #fee; }
body		{ font: 13pt "Myriad Pro", Helvetica;
		  /* padding: 30px; */
		  /* margin: 5px 0 0 2%; */
		}
#left		{ float: left; margin-left: 15px; margin-top: 15px; width: 200px; background: #eff; }
#fincom		{ font-weight: bold; font-size: 1em; }
#inventar	{ font-weight: bold; font-size: 2.5em; margin-bottom: 50px; }
#main		{ float: left; margin-left: 15px; margin-top: 15px; background: #f8ffff; }
h2 .func 	{ font-size:  0.5em; }
table .part 	{ margin-top: 0.5em; border-spacing: 0.3em; border: 0}
th 		{ font-size:  0.8em; color: #922; background: #f8f8f8 }
.part.desc 	{ text-align:left;  padding-top:   10px; }
.part.serial 	{ text-align:left;  padding-right: 10px; }
.part.datev_id 	{ text-align:left;  padding-right: 10px; }
.part.price 	{ text-align:right; padding-right: 10px; }
.odd		{ background: #f8ffff; }
.even		{ background: #f4fafa; }
#form_buttons   { overflow: hidden; }
#form_buttons input { float: right; }
#form_buttons #commit { font-weight: bold; }
.bold   { font-weight: bold; }
  </style>
</head>
<body>
<div id="left">
  <div id="fincom">
    <%= link_to "financial.com AG", "http://financial.com" %>
  </div>
  <div id="inventar">
    <%= link_to "Inventar", root_path %>
  </div>
  <div id="tablinks">
    <%= link_to "Parts", parts_path %>
  </div>
</div>
<div id="main">
  <div id="flash">
    <% flash.each do |key, value| %>
      <div class='flash' id='<%= key %>'>
        <%= value %>
      </div>
    <% end %>
  </div>
  <div id="data">
    <%= yield %>
  </div>
</div>
</body>
</html>
