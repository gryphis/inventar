<!DOCTYPE html>
<html>
<head>
  <title><%= @title || "Inventar" %></title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <style type="text/css">
h2 a { font-size:0.5em; }
table.part { margin-top:0.5em; border-spacing:0.3em; }
th { font-size:0.8em; color:#773377; }
.part.desc { text-align:left; }
.part.serial { text-align:left; }
.part.datev_id { text-align:left; }
.part.price { text-align:right; }
  </style>
</head>
<body>
<table div="main">
  <tr>
    <td>financial.com AG<br/><h1><%= link_to "Inventar", root_path %></h1></td>
  </tr>
  <tr>
    <td>
      <% flash.each do |key, value| %>
        <div class='flash' id='<%= key %>'>
	 <%= value %>
	</div>
      <% end %>
    </td>	
  </tr>
  <tr>
    <td>
      <%= yield %>
    </td>
  </tr>
</body>
</html>
